{
  "$schema": "https://turborepo.com/schema.json",
  "tasks": {
    "package": {
      "dependsOn": ["@wrkspc/types#package"]
    },

    "test": {
      "dependsOn": ["@wrkspc/types#build"],
      "inputs": [
        "$TURBO_DEFAULT$",
        "./Cargo.toml",
        "./src/**/*.rs",
        "./tests/**/*.rs"
      ]
    }
  },
  "extends": ["//"]
}
